# 命名規約

このリポジトリの識別子は日本語で命名する。接頭辞で種別を表す。

## 接頭辞の一覧

| 対象 | 形式 | 例 |
|---|---|---|
| メソッド(原則) | `V_名前` | `V_構築_ユニティググラフ` |
| メソッド(取得) | `Get_名前` | `Get_カバレッジ` |
| メソッド(設定) | `Set_名前` | `Set_推定Phredオフセット` |
| 引数 | `p_名前` | `p_kmer長` |
| ローカル変数 | `l_名前` | `l_合計長` |
| public プロパティ | `A_名前` | `A_コピー数` |
| private フィールド | `_名前` | `_信頼できるkmer` |

## 各種別の細則

### メソッド

- private メソッドも同じ規約に従う。
- 「を」「に」といった助詞はなるべく使わない。引数に渡す変数名自体が説明に
  なっているため、変換元は名前に含めない。
  - 悪い例: `V_バイト列を文字列に変換`
  - 良い例: `V_変換_文字列`
- **ローカル関数(メソッド内で宣言する関数)は使わない。** メソッドとして
  外部に宣言し直したうえで、メソッドの命名規約を適用する。

### 引数

- 無条件で `p_` を付ける。`out` 引数も同様。

### ローカル変数

- ループカウンタ(`i`, `j` のような1文字の添字)は例外とし、接頭辞を付けない。
- ラムダの引数は、`x` のような1文字なら接頭辞なしでよい。説明的な名前を
  付ける場合は `l_` を付ける。

### プロパティ

- public プロパティは `A_` を付ける。
- **private プロパティは、可能な限り private フィールドに変換する。**
  プロパティである必要がないため。getter/setter に処理を持たせたい場合のみ
  プロパティのまま残し、その場合は**接頭辞を付けず**に日本語名を付ける。
- record の位置パラメータおよびタプルの要素名は public プロパティとして
  扱い、`A_` を付ける。

### 定数

- `Consts` の `public const` / `static readonly` は**接頭辞なし**で日本語化する。
- `using static Tsumiki.Common.Consts;` は使わず、常に `Consts.～` の形で
  参照する(どこ由来の定数か一目で分かるようにするため)。

## 型名

- クラス名・インターフェース名は原則として変更しない(英語のまま)。
- ただし**値を持つだけの型(エンティティ)と enum は日本語化する。**
- **入れ子の型は可能な限り避け、`Model/` 配下へ独立した型として展開する。**
  現状で入れ子になっているもの(`ErrorCorrector.CorrectionResult`,
  `GapFiller.Stats`, `GapFiller.FillOutcome`, `CopyNumberEstimator.Result`,
  `AssemblyValidator.Result`, `BeamSearchExtender.State` など)が対象。
- `KmerKey` と `Parameters` は型名を変更しない(参照箇所が多く、`KmerKey` は
  振る舞いも持つため)。ただし中身のメンバは規約に従って日本語化する。

## 適用範囲

- 本体(`Tsumiki`)とテスト(`Tsumiki.Tests`)の両方に適用する。

## 例外(.NET 側が名前を決めているもの)

以下は変更すると動作しなくなるため、**メソッド名は英語のまま残し、
中身の引数名・ローカル変数名だけ規約に合わせる**。

- `Main`(エントリポイント)
- `Dispose`(`IDisposable`)
- `Compare`(`IComparer<T>`)、`Equals`(`IEquatable<T>` / `object`)、`GetHashCode`、`ToString`
